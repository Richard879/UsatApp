/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import { UsatApp } from './src/UsatApp';

AppRegistry.registerComponent(appName, () => UsatApp);